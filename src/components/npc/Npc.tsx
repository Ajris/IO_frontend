import React from 'react';

export interface NpcProps {
    text: string;
    position: [number, number];
}

const Npc = ({text, position}: NpcProps) => {
    return (
        <div className="character">
            
            
        </div>
    )
}

export default Npc;

